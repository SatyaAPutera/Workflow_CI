# Kriteria 3 CI/CD — Docker Hub

Docker Hub repository for the CI/CD image:

- Repository: https://hub.docker.com/repository/docker/satyaap/kriteria_3/general
- Image name: satyaap/kriteria_3:latest

Quick usage:

```powershell
# Pull the image
docker pull satyaap/kriteria_3:latest

# Run (example)
docker run --rm satyaap/kriteria_3:latest
```

If you prefer the public repository URL format, use:

- Public link: https://hub.docker.com/r/satyaap/kriteria_3
